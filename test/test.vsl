FUNC main()
{
    // This is a comment
	VAR i
	i := 0
	VAR a, b, c
	a := 8
	b := 2
	c := a / b
    WHILE 11 - i
	DO
	{
		PRINT "f( ", i, " ) = ", f(i),  "\n"
		i := i + 1
	}
	DONE
}
FUNC f(n)
{
	IF n
	THEN
		RETURN n * f(n - 1)
	ELSE
		RETURN 1
	FI
}
